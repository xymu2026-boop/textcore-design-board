# INBOX · Codex · A3 工作台 / 课稿库 / 知识资产页打磨

> 前端收尾步骤 5。**只碰 `apps/web/`**。结果写 `handoff/OUTBOX_CODEX.md`，`LOG.md` 追加一行。**不提交 git**。
> 当前分支 `frontend-polish`。对照黄金参照 `docs/prototype/`（`index.html`/`styles.css`/`app.js`，只读）。

## 背景
详情页（A2.1–A2.3）已完成。本步打磨其余三个页面，向 demo 看齐。**详情页不要再大改**。

## 范围（只动 apps/web，对照 docs/prototype）
1. **工作台（首页）**
   - 上传卡片（拖拽区、图标、主副标题、"选择 Word 文件"按钮）尺寸/留白/质感对齐 demo。
   - 右侧"处理进度"步骤列表 + "最近处理"历史区版式对齐 demo。
   - 上传后处理中 SSE 进度展示（已有 ProgressPanel，打磨视觉即可）。
2. **课稿库（列表页）**
   - 表格行高/列对齐/状态徽标（已完成/处理中/有复核）样式对齐 demo。
   - 搜索框、状态筛选样式打磨（筛选可做前端过滤）。
   - 空态/加载失败态（A1 已加）保持。
3. **知识资产页**
   - 按你审查意见：**本步只做"从课程聚合的入口 + 空态"过渡版**，不做复杂知识库（真聚合留阶段三 C1 后端 API）。
   - 二级 Tab（方法卡片库/词汇生词本/佳句素材册）样式对齐 demo；无聚合数据时显示清晰空态/入口，不硬凑假资产。

## 不做 / 边界
- 不再大改详情页（A2 已完成）。
- 不碰 apps/api、textcore、schemas、prompts、docs/prototype（只读）。
- 不引入硬编码业务数据；知识资产页无数据就空态，不造假。
- 不提交 git。

## 验收标准
- 工作台、课稿库、知识资产页视觉明显接近 demo。
- 上传流程：选文件→处理中进度→完成入列 顺畅（真实 API）。
- 课稿库 6 篇真实课程渲染、筛选/搜索可用、状态徽标正确。
- 知识资产页有清晰入口/空态，不报错不造假。
- `cd apps/web && npm run check` + `npm run build` 通过。

## 完成后
- `OUTBOX_CODEX.md`：三页各打磨了什么、与 demo 差异、知识资产页空态怎么处理、check/build 结果。
- `LOG.md` 追加：`[时间] CODEX: A3 工作台/课稿库/资产页打磨 完成`。
